# 项目当前情况 —— 面向大学生的多模态课程知识库与 AI 辅导系统（Mini-NotebookLM）

> **本文档的定位**：本项目文档体系采用「当前情况 → 开发目标 → 实施手册」三件套滚动维护。本文是其中的**「当前情况」**——描述项目代码**此刻已经实现到什么程度**，必须与代码库保持一致；新一轮开发完成后同步更新本文。
>
> - 当前情况（本文）：`项目当前情况.md`
> - 下一步要做成什么样：`下一步开发目标.md`（需求规格）
> - 从当前情况到目标怎么做：`开发实施手册.md`
>
> **当前版本：v1.2.0（已通过用户验收）**。最后更新：2026-05-31。
>
> 本文的技术细节以项目内两份技术文档为事实依据：
> - [在线API输出文件格式（SaaS推断版）.md](.\在线API输出文件格式（SaaS推断版）.md) —— MinerU 输出格式
> - [MinerU to RAG Pipeline 架构设计与数据流方案.md](.\MinerU to RAG Pipeline 架构设计与数据流方案.md) —— 解析入库方案

---

## 1、项目概述

本项目是一款面向大学生课程学习场景的**本地化多模态个人知识库与 AI 辅导系统**（Mini-NotebookLM）。它由两部分组成：

1. **通用多模态 AI 知识库底座**（模块一~六）：PDF/PPT/Word/图片的高保真解析、结构化切片、向量 + 关键词混合检索、重排序、多模态问答与高亮溯源；v1.2.0 起原生支持 `.txt`（课堂录音转写）/`.md`（讲义、笔记）两种纯文本类型，并新增「本地文件夹绑定」模式。
2. **场景 AI Agent 模块**：在底座之上针对大学生痛点提供垂直辅学功能——**模块七 AI 伴学·课后复盘**（已实现）、**模块九 AI 管家·课程信息卡片**（已实现）；**模块八 AI 考官·出卷判分**与音视频转写作为提升项，本期未实现。

系统采用前后端分离架构，在本地 Windows 环境运行，核心解析、向量化、重排序、问答能力依托云端 API。

**技术路线**：MinerU 高保真解析 → 自研 IR 标准化中间层（重建文档树、注入标题路径、坐标锚点）→ Parent/Child 结构感知切片 → 向量 + BM25 双路混合召回 → qwen3-rerank 重排 → 多模态旗舰模型问答。

**约束**：单人 + AI 编程助手开发；坚持 KISS，不引入 LangChain 等重封装框架，优先原生 SDK 与透明数据流。

### 当前完成状态（2026-05-31）

- 基座 v1.0.0 / v1.1.0：核心 RAG 链路 100% 跑通；MinerU 格式漂移监控系统就位。
- **v1.2.0（本文版本，已验收）**：文件夹绑定基座、多轮对话基座（conversations/messages + fork + 思维链）、模块七（课后复盘）、模块九（课程管家）全部实现并通过用户验收。
- 质量基线：后端脚本式测试 `test_v120.py` **91/91 通过**；`basedpyright` standard 模式 **0 error**；前端 `tsc -p tsconfig.app.json` **0 error**、生产构建通过、ESLint 通过；MinerU 格式探针复测 **15/15 符合**。
- 验收时用户记录了若干小问题，留待下一版修复（详见 §6）。

---

## 2、已实现的功能模块

系统划分为九大模块。模块一~六为基础 RAG 链路，模块七 / 九为场景模块（v1.2.0 已实现），模块八为提升项（仅建表，未实现）。

### 模块一：多格式资料接入与异步解析 ✅

**支持文件类型**：
- 文档/图片：`pdf`、`ppt/pptx`、`doc/docx`、`png/jpg/jpeg`、`xlsx/xls` —— 统一走 MinerU 在线 API（`/api/v4` 批量精准解析，`model_version=vlm`）。
- **纯文本（v1.2.0 新增）**：`txt`（课堂录音转写）、`md`（AI 讲义 / 用户笔记）—— **跳过 MinerU**，直接 UTF-8 读取，status 直接置 `text_only`。

**两种资料接入模式**：
- **上传模式**：浏览器逐个/批量上传文件至 KB（保留 `relative_path` 逻辑路径）。
- **文件夹绑定模式（v1.2.0 新增，课程 KB 必用）**：创建 KB 时填本地目录绝对路径（`knowledge_bases.bound_folder_path`），点「同步」按钮（`POST /api/kb/{kb_id}/sync-folder`，`folder_sync_service.py`）扫描目录登记文件；**不自动调用 MinerU**（省配额），由用户在文件页按需点「解析」；目录约定结构见模块七。消失的文件标 `missing` 不立即删库。

**解析流水线**（`pipeline_service.py`，16 步异步编排）：
1. `[A]` 申请 MinerU 批量预签名上传 URL → `[B]` PUT 上传 OSS → `[C]` 轮询任务取 `full_zip_url` → `[D]` 下载 ZIP
2. `[E]` 解压 + bundle 文件角色识别（`bundle_parser.py`）
3. `[E+]` **MinerU 格式严格审计**（`format_checker.py` → `data/format_probe_log.jsonl`）
4. `[F]` 归一化 `content_list_v2.json` → `IRBlock[]`（`normalizer.py`）
5. `[G]` DOM 重建 + `header_path` 注入（`dom_builder.py`）→ `[H]` 脚注关联 → `[I]` 读 `layout.json`
6. `[J]` 写 `document_ir.json` → `[IR-V]` IR 校验（`ir_validator.py`）
7. `[K-vlm]` 多模态富化：图片描述 / 表格摘要（`enricher.py`，VLM 模型可配）
8. `[L]` Parent Chunk → `[M]` Child Chunk → `[Chunk-V]` 校验 → `[N]` 向量化 → `[O]` Qdrant+SQLite 入库 → `[P]` JSONL 落盘 → 更新文档状态

**未实现**：音视频转写（飞书妙记 / 通义听悟）—— 提升项。

### 模块二：MinerU 数据解析与标准化 IR ✅

双层「宽进严出」架构：
- **Raw 层**（`models_raw_mineru.py`，`extra="allow"`）接收所有字段，未知字段触发 WARNING。
- **IR 层**（`models_ir.py`）严格 Pydantic 校验、枚举类型、坐标范围校验。

Normalizer 块类型映射：`title→title`、`paragraph→paragraph`、`list/index→list`、`image→image`、`chart→image`(chart_image)、`table→table`、`equation_interline→equation`、`code→code`、页眉/页脚/页码/脚注→同名辅助块。

Office 原生解析（DOCX/PPTX）特殊处理：所有块 `bbox` 键缺失，用 `[0,0,0,0]` 静默占位，校验器跳过 sentinel bbox；无 `origin.pdf`，前端隐藏「查看原文」。

落盘：`data/rag_output/{doc_id}/` 下 `document_ir.json` / `document_ir_enriched.json` / `parent_chunks.jsonl` / `child_chunks.jsonl`。

### 模块三：结构感知切片与多模态富化 ✅

- **Parent Chunk**（`parent_chunker.py`）：每个 `IRSection` 一个 ParentChunk，跳过 synthetic root；图片/表格输出 `"[图片: caption]"` / `"[表格: caption]"` 占位。
- **Child Chunk**（`child_chunker.py`）：原子块（list/code/image/table/equation）整体不拆；paragraph 滑动窗口切分（`max_tokens=250`/`min_tokens=150`/`overlap=0.15`）；`embedding_text = "{header_path}\n{text}"`。
- **富化**（`enricher.py`，并发 3）：图片 base64 → VLM 中文描述；表格 HTML → VLM 中文摘要；结果回流 `embedding_text` 增强检索。

### 模块四：索引、入库与混合检索 ✅

- **向量**（`embedding_service.py`）：`text-embedding-v4`（百炼，1024 维），Child Chunk 存 Qdrant（payload 含完整元数据）。
- **关键词**（`database.py`）：SQLite FTS5 虚拟表 `child_chunks_fts`（`unicode61`），触发器自动同步。
- **混合检索**（`retrieval_service.py`）：向量召回 + FTS5/BM25 召回并行，RRF 融合（`k=60`），融合后 top_k=15。
- **重排序**（`rerank_service.py`）：`qwen3-rerank`（DashScope 专有端点，不可换 Provider），top_k=15 → top_n=5。

### 模块五：多模态问答与提示词编排 ✅

- **QA Service**（`qa_service.py`）：上下文 `[来源{i}] {page_info} · {path}\n{retrieval_text}`；`[system, user]` 两条消息。
- **多 Provider**：`QA_BASE_URL`/`QA_API_KEY`/`QA_MODEL` 切换任意 OpenAI 兼容 Provider（默认百炼 `qwen-plus`，可切 DeepSeek/OpenAI/Moonshot）。
- **SSE 流式**：事件 `citations → thinking（可选）→ delta（流）→ end`。
- **提示词目录**（`backend/app/prompts/`，v1.2.0 外置）：5 个模板 + 加载器，`GET /api/settings/prompts` 只读展示、`POST /api/settings/prompts/reload` 热重载。

### 模块六：前端交互与溯源展示 ✅

页面结构（React Router v7，嵌套路由，`KBLayout` 提供二级侧边栏）：
- `/` → `KnowledgeBasePage`（知识库首页，卡片网格；课程 KB 卡片展示场景模块入口）
- `/kb/:kbId` → `KBLayout`（二级侧边栏：文件管理 / 通用对话 / 课后复习 / 课程管家）
  - `files` → `KBFilesPage`（上传/解析/删除/批量；txt/md 原文查看；解析状态轮询；MinerU 警告卡片）
  - `chat` `chat/:conversationId` → `ChatPage`（SSE 流式问答 + 引用面板 + PDF bbox 高亮）
  - `review` `review/:conversationId` → `ReviewPage`（模块七，三栏）
  - `info` → `CourseInfoPage`（模块九）
- `/tasks` → `TasksPage`；`/settings` → `SettingsPage`；`*` → 重定向 `/`

**ChatPage**：`fetch + ReadableStream` 解析 SSE；引用面板展示章节路径/页码/相关性分数，可展开原文；「查看原文」用 `react-pdf` 加载 `origin.pdf` 叠加 bbox 高亮；非 PDF 隐藏该按钮；`react-markdown + remark-gfm` + 自定义 `.md-prose`。

### 模块七：AI 伴学·课后复盘 ✅（v1.2.0 实现）

**定位**：基于课堂录音转写自动生成「老师口吻」的逐节讲义，并在同一对话上下文内多轮答疑、对话分叉。

**目录约定**（课程 KB 绑定的本地文件夹）：
```
<课程名称>/
  课堂录音/<YYMMDD>/
      <课程>YYMMDD第NN节.txt          ← 录音转写（每小节一份）
      <课程>YYMMDD第NN节课堂要点.md   ← AI 生成的讲义（保存后出现）
  课件/  作业/  通知/                  ← 模块九检索用
```

**实现**：
- 服务：`lecture_review_service.py` + `conversation_service.py`（多轮对话基座）。
- API：`api/review.py`（`dates` / `sections` / `generate` / `save-notes` / `notes` / `conversations` / `followup`）+ `api/conversations.py`（CRUD / `send` / `fork`）。
- 前端：`ReviewPage.tsx`，三栏（会话列表 / 聊天主区 / 上下文面板）。
- 提示词：`lecture_review_section_first.md`、`lecture_review_section_subsequent.md`、`lecture_review_followup_system.md`。
- 流程：选日期 → 读该日期目录全部 `*.txt` 按「第 N 节」排序 → 同一 conversation 内逐节调用 LLM 生成讲义（首节/后续节模板）→ 「保存到本地」写 `.md` → 同会话追问 → 对任意 assistant 消息 **fork**（子会话不污染主线）→ **思维链开关**（思考与回答均流式；追问消息支持「展开/收起思维链」）。

### 模块八：AI 考官·出卷判分 ⏳（提升项，未实现）

仅数据库就绪（`exam_questions` / `exam_papers` / `exam_submissions`），无后端服务、无前端页面。未来方向：Markdown 题库 → AI 组卷（含数值变形）→ 答卷拍照 **MinerU 解析 + LLM 逐题对照判分**。

### 模块九：AI 管家·课程信息卡片 ✅（v1.2.0 实现）

**定位**：从课程 KB 已绑定资料中自动抽取关键信息生成结构化卡片，并对临近截止日防遗漏提醒。

**实现**：
- 服务：`course_info_service.py`。
- API：`api/course_info.py`（`generate` / `get` / `upcoming-deadlines` / `delete` / `chat`）。
- 前端：`CourseInfoPage.tsx`（卡片网格 + 重新生成 + 追问聊天面板）。
- 提示词：`course_info_extract_system.md`、`course_info_chat_system.md`。
- 流程：5 个固定查询（考核方式 / 作业 DL / 考试时间 / 老师联系方式 / 重要通知）触发混合检索 → LLM **一次结构化 JSON 抽取** → 写 `course_info_cards`（含 `deadlines_normalized` JSON）→ 卡片展示 → 基于卡片在多轮对话基座上追问。
- **截止日 banner**：进入课程 KB 任何页面，若未来 7 天内有 deadline → 顶部黄色提醒。
- **`days_left` 实时重算**：卡片读取时（`get_card`）按当天从权威的 ISO `date` 字段重算 `days_left`，避免生成时算好的值随时间过期（v1.2.0 回归修复，`test_v120.py` 有对应回归用例）。

---

## 3、运行环境与 API Key

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 本地运行 |
| Python | 3.11+，包管理 **uv**（非 pip） |
| Node.js | 20+ |
| 后端 | FastAPI + Uvicorn，`127.0.0.1:8000` |
| 向量库 | Qdrant 本地**文件模式**（`data/qdrant_storage/`，**单进程文件锁** —— 同一时刻只能一个进程持有） |
| 关系库 | SQLite + FTS5（内置） |
| 浏览器 | Chrome / Edge 120+ |

**必填 API Key**（`backend/.env`，模板见 `backend/.env.example`）：

| API | 环境变量 | 用途 |
|-----|---------|------|
| MinerU 在线 API | `MINERU_API_KEY` | 文档解析 |
| 阿里云百炼 DashScope | `ALIBABA_CLOUD_ACCESS_KEY_SECRET` | 嵌入 / 重排 / VLM |
| QA 模型（可选） | `QA_BASE_URL` + `QA_API_KEY` + `QA_MODEL` | 问答 Provider 切换，不填用 DashScope `qwen-plus` |
| VLM 模型（可选） | `VLM_MODEL` | 多模态富化模型（默认 `qwen-vl-plus`） |

> `config.py` 中所有 key `default=""`，无 `.env` 也能启动（仅真机解析/问答不可用）。

---

## 4、代码库技术描述（接手开发者向）

### 4.1 目录结构

```
mini-notebooklm-copilot/
├── backend/app/
│   ├── adapters/        bundle_parser / normalizer / dom_builder / footnote_linker / format_checker
│   ├── api/             health / kb / documents / chat / tasks / settings
│   │                    conversations / course_info / review      ← v1.2.0 新增
│   ├── chunkers/        parent_chunker / child_chunker
│   ├── db/              database（DDL + 懒迁移 + get_db）/ qdrant_client
│   ├── enrichers/       enricher（VLM 图片描述 / 表格摘要）
│   ├── models/          models_raw_mineru / models_ir / models_chunk
│   ├── prompts/         5 个 .md 提示词模板 + 加载器           ← v1.2.0 新增
│   ├── services/        pipeline / mineru_client / embedding / index / retrieval / rerank / qa
│   │                    conversation / course_info / lecture_review / folder_sync  ← v1.2.0 新增
│   ├── validators/      ir_validator / chunk_validator
│   ├── writers/         ir_writer / chunk_writer
│   ├── config.py        pydantic-settings，多 Provider 属性
│   └── main.py          FastAPI 入口，lifespan 初始化 DB+Qdrant
├── backend/tools/       mineru_format_probe.py（格式探针，离线/在线）
├── backend/test_*.py    脚本式端到端测试（test_api / test_v120 / test_stage2~4）
├── frontend/src/
│   ├── api/             client.ts / types.ts
│   ├── components/      Layout（主导航）/ KBLayout（二级侧边栏）/ ui（shadcn）
│   ├── pages/           KnowledgeBasePage / KBFilesPage / ChatPage / TasksPage / SettingsPage
│   │                    ReviewPage / CourseInfoPage                ← v1.2.0 新增
│   └── App.tsx          React Router v7（嵌套路由）
├── data/                运行时数据（.gitignore；format_probe_log.jsonl 不入库）
└── doc/                 项目文档（见 §7）
```

### 4.2 SQLite 数据库（13 张业务表 + 1 张 FTS5 虚拟表）

`data/sqlite/mini_notebooklm.db`。`init_db()` 启动时通过 `ALTER TABLE ADD COLUMN` 懒迁移安全升级旧库。

**RAG 核心表**：`knowledge_bases`（含 v1.2.0 `bound_folder_path`）、`documents`（含 v1.2.0 `bound_file_path` / `folder_category`，`source_format`、`status`、`warnings`、`origin_pdf_path`、`ir_path`）、`tasks`、`parent_chunks`、`child_chunks`、`assets`、`child_chunks_fts`(FTS5 虚拟表)。

**场景模块表**：
- `review_notes`（模块七笔记，实际讲义落磁盘 `.md`）
- `exam_questions` / `exam_papers` / `exam_submissions`（模块八，仅建表未用）
- `course_info_cards`（模块九，含 `deadlines_normalized` JSON）
- **`conversations`（v1.2.0）**：`kb_id` / `scenario` / `parent_conversation_id` / `fork_from_message_id` / `enable_thinking` / `title` / 时间戳
- **`messages`（v1.2.0）**：`conversation_id` / `role` / `content` / `thinking` / `sequence_num` / `citations` / `metadata`

### 4.3 后端 API（prefix `/api`，Swagger `/docs`）

| 分组 | 端点 |
|------|------|
| health | `GET /health` |
| kb | `GET/POST /kb`、`GET/PATCH/DELETE /kb/{id}`、**`POST /kb/{id}/sync-folder`**（v1.2.0） |
| documents | `POST /{kb}/upload`、`GET /{kb}`、`GET/POST(parse)/DELETE /{kb}/{doc}`、`GET /{kb}/{doc}/origin-pdf`、**`GET /{kb}/{doc}/raw-text`**（v1.2.0 读 txt/md） |
| chat | `POST /chat/{kb}`（SSE）、`POST /chat/{kb}/search`（纯检索） |
| **conversations**（v1.2.0） | `POST/GET /conversations`、`GET/PATCH/DELETE /conversations/{id}`、`POST /conversations/{id}/send`、`POST /conversations/{id}/fork` |
| **course_info**（v1.2.0） | `POST /{kb}/generate`、`GET /{kb}`、`GET /{kb}/upcoming-deadlines`、`DELETE /{kb}`、`POST /{kb}/chat` |
| **review**（v1.2.0） | `GET /{kb}/dates`、`GET /{kb}/sections`、`POST /{kb}/generate`、`POST /{kb}/save-notes`、`GET /{kb}/notes`、`GET /{kb}/conversations`、`POST /{kb}/followup` |
| **settings**（v1.2.0） | `GET /settings/prompts`、`POST /settings/prompts/reload` |
| tasks | `GET /tasks`、`GET /tasks/doc/{id}`、`GET /tasks/{id}` |

### 4.4 核心 AI 服务

| 功能 | 服务 | 模型 | 可切换 |
|------|------|------|--------|
| 文档解析 | MinerU SaaS | `vlm` | 否 |
| 文本向量化 | 百炼 | `text-embedding-v4`（1024 维） | 否 |
| 重排序 | 百炼 | `qwen3-rerank` | 否 |
| 多模态富化 | 百炼 | `qwen-vl-plus`（`VLM_MODEL` 可配） | 换模型 |
| 最终问答 | 多 Provider | 默认 `qwen-plus`（`QA_MODEL` 可配） | 换 Provider |

### 4.5 MinerU 格式漂移监控

`format_checker.check_bundle` 在流水线 [E+] 对每次解析做字段/文件层面的严格审计；有偏差才追加 `data/format_probe_log.jsonl`（无偏差不写）。独立探针 `tools/mineru_format_probe.py`（`--online` 消耗配额；退出码 0/1/2/3 = clean/error/warning/info）。**盯住该 jsonl 文件增长**；出现新格式时同步更新 normalizer/format_checker 已知字段集合与两份 MinerU 文档（见 §7）。

### 4.6 前端关键技术点

- SSE：`fetch + ReadableStream`（非 EventSource），支持 `AbortController`，事件 `citations/delta/thinking/end/error`。
- PDF bbox 高亮：`react-pdf` 渲染 + 绝对定位 `<div>` 高亮框，`bbox_norm1000`（0–1000）换算页面百分比。
- Markdown：自定义 `.md-prose`（不依赖 `@tailwindcss/typography`，规避 Tailwind v4 preflight 列表重置）。
- 思维链：`ReviewPage` 追问消息可「展开/收起思维链」，`upsertAssistant` 同时处理 delta 与 thinking 事件。

---

## 5、测试与质量基线

- **后端**：`test_v120.py` **91/91 通过**（含 `days_left` 实时重算回归用例）；`test_api.py` / `test_stage2~4.py` 端到端。运行前**必须停掉 uvicorn**（Qdrant 单进程文件锁）。
- **类型检查**：仓库根 `uv run --project backend basedpyright`（standard 模式）**0 error**；前端 `npx tsc -p tsconfig.app.json --noEmit` **0 error**。
- **Lint/格式化**：`ruff check/format`；前端 `eslint`。
- **构建**：前端 `npm run build` 通过。
- **格式探针**：复测 **15/15** 符合。
- **真机 e2e**：树形文件管理、RAG 流式问答（引用/重排分数/fork）、模块九卡片 + DDL banner 均已浏览器验证。

---

## 6、已知限制与待办

| 类别 | 说明 |
|------|------|
| 验收小问题 | 用户 v1.2.0 验收时记录了若干小问题，留待下一版集中修复（具体清单待用户给出） |
| 模块八 | AI 出卷判分仅建表未实现（提升项） |
| 音视频转写 | 未实现（飞书妙记 / 通义听悟，提升项） |
| FTS5 中文分词 | `unicode61` 对中文召回有限，可考虑 jieba |
| 重排序 Provider | `qwen3-rerank` 仅 DashScope，不可切换 |

下一步要做成什么样、以及怎么做，分别见 `下一步开发目标.md` 与 `开发实施手册.md`。

---

## 7、项目文档体系

```
doc/
├── 项目当前情况.md            ← 本文：项目此刻已实现到什么程度（与代码一致）
├── 下一步开发目标.md          ← 需求规格 / 下一轮要做成的样子
├── 开发实施手册.md            ← 从当前情况到目标的逐步开发计划
├── MinerU to RAG Pipeline 架构设计与数据流方案.md  ← 当前在用的 MinerU 解析/入库方案
├── 在线API输出文件格式（SaaS推断版）.md            ← 当前所基于的 MinerU 输出格式推断
├── mineru/                    ← 从 MinerU 官网下载的文档（项目的事实来源）
│   ├── MinerU API 文档（新的）.md   ← 官网新版 API 文档（原样保留）
│   └── 输出文件格式（新的）.md       ← 官网声称的输出格式（更新滞后、有误，仅供参考）
└── 阿里云模型/                ← 阿里云百炼官方文档（向量化 / VLM）
```

**维护约定**：每轮开发完成后更新「当前情况 / 下一步开发目标 / 开发实施手册」三件套 + README + RELEASE_NOTES；MinerU API 出现未知/错误字段时更新《在线API输出文件格式（SaaS推断版）》；解析入库工作流变化时更新《MinerU to RAG Pipeline 架构设计与数据流方案》。

---

*课程设计 · 程序设计实训 · 本科 · 最后更新：2026-05-31（v1.2.0 已验收）*
